<?php

/**
 * Define image sizes
 *
 * Define custom image sizes for use throughout the theme. Note that images will
 * not be resized automatically if they are uploaded before an image size has
 * been defined.
 */
// add_image_size('apollo-small-image', 480, 480, true);
// add_image_size('apollo-medium-image', 800, 800, false);
